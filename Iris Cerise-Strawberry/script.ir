;bitmap = Bitmap.new("image/kurumi.jpg")
;sprite = Sprite.new()
;sprite.bitmap = bitmap
;flag = true
if(true, 0) {
/*
  	if(flag) {
		;sprite.x += 1
		;sprite.y += 1
		if(sprite.x == 60) {
			;flag = false
		}
	} else {
		;sprite.x -= 1
		;sprite.y -= 1	
		if(sprite.x == 0) {
			;flag = true
		}
	}
*/
	if(Input.keyboard_press(19)) {
		;sprite.x -= 1
	} elseif(Input.keyboard_press(20)) {
		;sprite.x += 1
	}
	;Input.update()
	;Graphics.update()
}