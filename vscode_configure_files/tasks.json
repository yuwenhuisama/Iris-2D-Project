{
    "tasks": [
        {
            "type": "shell",
            "label": "clang build active file",
            "command": "/usr/local/clang",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/local"
            }
        }
    ],
    "version": "2.0.0"
}